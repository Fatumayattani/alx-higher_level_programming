#!/usr/bin/python3

text_indentation = __import__('5-text_indentation').text_indentation
text_indentation("""Importing a module in Python is done using the import statement. The __import__() function is a lower-level function that is used to import a module. It takes the name of the module to import as a string and returns the module object""")
